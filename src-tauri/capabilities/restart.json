{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "restart",
  "description": "Allow the local Flask UI to restart the desktop app",
  "windows": ["main"],
  "remote": { "urls": ["http://127.0.0.1:*"] },
  "permissions": ["allow-restart-app"]
}
